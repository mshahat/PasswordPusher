apiVersion: v1
kind: ServiceAccount
metadata:
  name: default
secrets:
  - name: registry-credentials
  - name: tap-registry
  - name: github-ssh
imagePullSecrets:
  - name: registry-credentials
  - name: tap-registry
